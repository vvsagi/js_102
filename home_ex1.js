// Write a function, checkIfArray, that takes an argument, someArray. The function shouldreturn true if the argument is an array and false if it is not.

function checkIfArray(arr) {
    if (arr.isArray()) {
        
    }
}